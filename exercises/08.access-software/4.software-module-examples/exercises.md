# Exercises for the Software module examples section 

1. Is "matplotlib" a separate module? 
2. Is there a module named "SciPy-bundle"? Check what is included in it if it exists. 
3. Find out how to load "SciPy-bundle" in a version compatible with a Python version 3.11.x (pick one). 
4. Can R be loaded directly? Does it have any "bundles" with extra R packages available? 
5. Is MATLAB installed? 
    - Which versions? 
    - How is it loaded? 
    - Does it have prerequisites or is it loaded directly? 
    - Try loading it! 
    - Is anything loaded with it? Check with ``module list`` 

