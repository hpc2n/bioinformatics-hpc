# Shell scripts and the Dockerfile must keep Unix line endings on every platform (a CRLF file fails inside the image)
Dockerfile text eol=lf
exam-practice-check text eol=lf
shims/* text eol=lf
dummy_data/* text eol=lf
tests/* text eol=lf
